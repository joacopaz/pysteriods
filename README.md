# pysteriods
Simple pygame testing of an Asteroids game implementation for Python

To run it:
- If you are a pythonista and have `uv` installed (any true pythonista should 😏) simply run `uv run pyinstaller --windowed --noconsole main.py` to compile the binary
- You can download the executable in the release section
